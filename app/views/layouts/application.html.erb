<!DOCTYPE html>
<html>
 <head>
   <title>ArtSphere</title>
   <meta name="viewport" content="width=device-width,initial-scale=1">
   <%= csrf_meta_tags %>
       <%= csp_meta_tag %>
   <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

   <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

  <%= javascript_importmap_tags %>
  <meta name="stripe-pk" content="<%= Rails.application.credentials.dig(:stripe, :pk) %>">
    <script src="https://js.stripe.com/v3/"></script>
 </head>

 <body class=" flex flex-col min-h-screen bg-gradient-to-r from-purple-300 via-indigo-200 to-purple-200">
 <%= render 'layouts/header' %>
   <main class=" flex-grow container mx-auto px-5 flex">
     <%= yield %>
   </main>
    <%= render 'layouts/footer' %>
  </body>
</html>